"""
Python五大数据容器分类总结：列表list
                        元组tuple
                        字符串str
                        集合set
                        字典dict

分类维度1：是否支持下标索引
✅ 支持下标（序列类型）：列表、元组、字符串
❌ 不支持下标（非序列类型）：集合、字典

分类维度2：是否允许存放重复元素
✅ 允许重复：列表、元组、字符串
❌ 不允许重复：集合（元素唯一）、字典（key不能重复，value可重复）

分类维度3：是否可修改（可变/不可变）
✅ 可变（能增删改）：列表、集合、字典
❌ 不可变（不能修改内部元素）：元组、字符串
"""

# 表格版注释，适合直接放PyCharm
"""
数据容器分类对照表
| 分类依据                | 支持                          | 不支持                      |
| ----------------------- | ----------------------------- | --------------------------- |
| 下标索引（序列/非序列） | 列表、元组、字符串（序列）    | 集合、字典（非序列）        |
| 重复元素                | 列表、元组、字符串            | 集合、字典key               |
| 可修改（可变类型）      | 列表、集合、字典              | 元组、字符串（不可变）      |
"""

"""
Python五大容器特点完整对比表
| 对比项       | 列表                             | 元组                             | 字符串                 | 集合                             | 字典                                                         |
| ------------ | -------------------------------- | -------------------------------- | ---------------------- | -------------------------------- | ------------------------------------------------------------ |
| 元素数量     | 支持多个                         | 支持多个                         | 支持多个               | 支持多个                         | 支持多个                                                     |
| 元素类型     | 任意类型                         | 任意类型                         | 仅字符                 | 任意不可变类型                   | Key：除字典外任意不可变类型；Value：任意类型                  |
| 下标索引     | 支持                             | 支持                             | 支持                   | 不支持                           | 不支持                                                       |
| 重复元素     | 支持                             | 支持                             | 支持                   | 不支持                           | Key不可重复，Value可以重复                                   |
| 可修改性     | 支持（可变）                     | 不支持（不可变）                 | 不支持（不可变）       | 支持（可变）                     | 支持（可变）                                                 |
| 数据有序     | 是                               | 是                               | 是                     | 否                               | 否                                                           |
| 使用场景     | 需要可修改、允许重复的批量数据   | 不可修改、允许重复的数据存储     | 专门存储文字字符串     | 需要去重、不关心顺序的数据存储   | 通过键Key快速查找对应值Value，适合存有映射关系的数据         |
"""

"""
| 容器   | 适用使用场景                                   |
| ------ | ---------------------------------------------- |
| 列表   | 可修改、允许重复的批量数据存储                 |
| 元组   | 不可修改、允许重复的批量数据存储               |
| 字符串 | 专门存储文字、字符内容                         |
| 集合   | 需要自动去重，不在意数据顺序的批量存储         |
| 字典   | 键值对应的数据，依靠Key快速查找对应Value       |
"""
